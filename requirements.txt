# ── Core ──────────────────────────────────────────────────────────────
pandas==2.2.3
numpy==2.1.3

# ── Excel & DB ────────────────────────────────────────────────────────
xlwings==0.33.6
openpyxl==3.1.5
xlrd==2.0.1            # legacy .xls (LLMS system exports)

# ── Analytics ─────────────────────────────────────────────────────────
scikit-learn==1.5.2
statsmodels==0.14.4
scipy==1.14.1          # statsmodels/scikit-learn dependency

# ── LLM agents (local Ollama) ─────────────────────────────────────────
ollama==0.4.4

# ── API server ────────────────────────────────────────────────────────
fastapi==0.115.5
uvicorn[standard]==0.32.1
pydantic==2.10.3
apscheduler==3.10.4

# ── Data feeds ────────────────────────────────────────────────────────
jugaad-data==0.31.1
feedparser==6.0.11

# ── Reporting ─────────────────────────────────────────────────────────
python-docx==1.1.2

# ── Dev / test ────────────────────────────────────────────────────────
pytest==8.3.4
httpx==0.27.2          # FastAPI TestClient transport

# ── Not currently used by src/ — uncomment if you need them ──────────
# torch==2.5.1               # deep learning (nothing imports it yet)
# prophet==1.1.6             # alternative forecaster
# SQLAlchemy==2.0.36         # ORM (raw sqlite3 is used instead)
# langgraph==0.2.59          # agent graphs (committee uses plain ollama)
# langgraph-checkpoint-sqlite==2.0.1
# langchain==0.3.10
# langchain-community==0.3.10
# nselib==1.3.0              # NSE data (jugaad-data is used instead)
# matplotlib==3.9.2          # plotting (no charts generated in code)
# python-decouple==3.8       # .env config (config is hardcoded for now)
